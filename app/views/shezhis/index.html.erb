<%= render 'form' %>
<table class="table table-striped table-hover shezhi-table">
  <tr>
    <th>名称</th>
    <th>类型</th>
    <th></th>
  </tr>

<% @shezhis.each do |shezhi| %>
  <tr class="danger">
    <td><%= shezhi.way %></td>
    <td><%= shezhi.lei %></td>
    <td><%= link_to '', edit_shezhi_path(shezhi),:class=>"glyphicon glyphicon-pencil" %>
        <%= link_to '', shezhi, method: :delete, data: { confirm: 'Are you sure?' },:class=>"glyphicon glyphicon-remove" %>
    </td>
  </tr>
<% end %>
</table>

<table class="table table-striped table-hover shezhi-table ">
  <tr>
    <th>名称</th>
    <th>类型</th>
    <th></th>
  </tr>

<% @shezhis.each do |shezhi| %>
  <tr class="danger">
    <td><%= shezhi.way %></td>
    <td><%= shezhi.lei %></td>
    <td><%= link_to '', edit_shezhi_path(shezhi),:class=>"glyphicon glyphicon-pencil" %>
        <%= link_to '', shezhi, method: :delete, data: { confirm: 'Are you sure?' },:class=>"glyphicon glyphicon-remove" %>
    </td>
  </tr>
<% end %>
</table>
